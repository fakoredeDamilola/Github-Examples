name: "Workflow commands"

on: ['push']

jobs: 
    my-job:
        runs-on: ubuntu-latest
        steps:
            - name: "My Step"
              run: |
                echo "Hello! {{github.action}}"